# Playwright MCP Snapshot Placeholder

Run `python -m src.agents.mcp_snapshot_agent --use-case part_creation` or use the Streamlit button to capture the required live Playwright MCP accessibility snapshot for the Part Creation use case.
